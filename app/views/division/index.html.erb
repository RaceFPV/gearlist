<br/>
<div class="col-sm-8 col-sm-offset-2">
    <h5 align="center"><i>This section is currently under active development. Creating and deleting divisions WILL NOT delete any associated assests, nor will it change the name of the division on currently exisiting assets. The current main use for this section is to allow for new divisions (and removal of old divisions) from the add asset page.</i></h5>
</div>
<div class="row">
  <div class="col-sm-10 col-sm-offset-1">
    <div class="panel panel-info">
    <div class="panel-heading">
      <h3 class="panel-title">Currently active divisions</h3>
    </div>
        <div class="panel-body dashboard-panel" align="center">
            <table id="" class="table table-striped table-bordered table-hover display" cellspacing="0" width="100%">
                <thead>
                    <tr>
                        <th>Division Name</th>
                        <th>Division Location</th>
                    </tr>
                </thead>
                <tbody>
                    <% if Division.count == 0 %>
                        <tr>
                            <td>
                                <h4>No divisions found.</h4>
                            </td>
                            <td>
                                <h4>No divisions found.</h4>
                            </td>
                        </tr>
                    <% else %>
                        <% Division.all.each do |x| %>
                            <tr>
                                <td>
                                    <%= x.name %>
                                </td>
                                <td>
                                    <%= x.location %> <%= link_to "Delete", delete_division_path(x.id), method: "delete", class: "btn btn-danger btn-sm", style: 'float: right', data: { confirm: "Are you sure?" } %>
                                </td>
                            </tr>
                        <% end %>
                    <% end %>
                </tbody>
            </table>
        </div>
    </div>
  </div>
  <div class="col-sm-6 col-sm-offset-3">
      <%= link_to 'Create new division', new_division_path, class: 'btn btn-success btn-block' %>
  </div>
</div>